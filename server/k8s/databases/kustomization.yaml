apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: databases-kustomization
  namespace: databases

namespace: databases

resources:
    - postgres.yaml
    - redis.yaml
    - rabbitmq.yaml
