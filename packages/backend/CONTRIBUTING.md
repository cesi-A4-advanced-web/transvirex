Si un nouveau service est ajouté, ou si des modifications sont apportées à la structure du projet, il est important d'exécuter `pnpm run prepare:docker` pour préparer les Dockerfiles avant de construire les images Docker.
De même en cas de modification du modèle de Dockerfile situé dans `scripts/`. 
