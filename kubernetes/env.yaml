apiVersion: v1
kind: ConfigMap
metadata:
    name: transvirex-config
    namespace: production
data:
    # ===================================
    # 🌐 Application Settings
    # ===================================
    NODE_ENV: 'production'
    APP_NAME: 'Transvirex ERP'

    # ===================================
    # 💻 Frontend & API
    # ===================================
    FRONTEND_HOST: 'localhost'
    FRONTEND_PORT: '3000'
    GATEWAY_SERVICE_PORT: '3000'
    API_HOST: 'localhost'
    API_PORT: '3000'
    AUTH_SERVICE_URL: 'http://transvirex-authentication:3000'
    BILLING_SERVICE_URL: 'http://transvirex-billing:3000'
    DELIVERY_SERVICE_URL: 'http://transvirex-delivery:3000'
    STOCK_SERVICE_URL: 'http://transvirex-stock:3000'
    USERS_SERVICE_URL: 'http://transvirex-users:3000'

    # ===================================
    # 🗄️ Database - PostgreSQL
    # ===================================
    POSTGRES_HOST: 'postgres'
    POSTGRES_PORT: '5432'
    POSTGRES_USER: 'postgres_user'
    POSTGRES_PASSWORD: 'postgres_password'
    POSTGRES_DB: 'transvirex'

    # ===================================
    # 📨 Message Queue - RabbitMQ
    # ===================================
    RABBITMQ_HOST: 'rabbitmq'
    RABBITMQ_PORT: '5672'
    RABBITMQ_USER: 'rabbitmq_user'
    RABBITMQ_PASSWORD: 'rabbitmq_password'
    RABBITMQ_MANAGEMENT_PORT: '15672'

    # ===================================
    # 🔴 Cache - Redis
    # ===================================
    REDIS_HOST: 'redis'
    REDIS_PORT: '6379'
    REDIS_PASSWORD: 'redis_password'
    REDIS_DB: '0'

    # ===================================
    # 🍃 Database - MongoDB
    # ===================================
    MONGODB_HOST: 'mongodb'
    MONGODB_PORT: '27017'
    MONGODB_USER: 'mongo_user'
    MONGODB_PASSWORD: 'mongo_password'
    MONGODB_DB: 'transvirex'

    # ===================================
    # 🔐 Security & Authentication
    # ===================================
    # JWT_SECRET=your-super-secret-jwt-key-change-in-production
    # JWT_EXPIRATION=24h
    # JWT_REFRESH_SECRET=your-super-secret-refresh-key-change-in-production
    # JWT_REFRESH_EXPIRATION=7d

    # ===================================
    # 📊 Logging & Monitoring
    # ===================================
    LOG_LEVEL: 'debug'

    # # ===================================
    # # 📧 Email (Optional - for notifications)
    # # ===================================
    # SMTP_HOST=smtp.gmail.com
    # SMTP_PORT=587
    # SMTP_USER=your-email@gmail.com
    # SMTP_PASSWORD=your-app-password
    # EMAIL_FROM=noreply@transvirex.com

    # ===================================
    # 🛠️ pgAdmin
    # ===================================
    PGADMIN_EMAIL: 'admin@transvirex.com'
    PGADMIN_PASSWORD: 'admin'

    # ===================================
    # ⚙️ Development Tools
    # ===================================
    DEBUG: '*'
