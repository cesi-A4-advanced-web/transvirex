apiVersion: apps/v1
kind: Deployment
metadata:
    name: transvirex-stock
    namespace: production
    labels:
        app: transvirex-stock
spec:
    replicas: 1
    selector:
        matchLabels:
            app: transvirex-stock
    template:
        metadata:
            labels:
                app: transvirex-stock
        spec:
            imagePullSecrets:
                - name: ghcr-secret
            containers:
                - name: stock
                  image: ghcr.io/cesi-a4-advanced-web/transvirex-stock-service:latest
                  imagePullPolicy: Always
                  env:
                      - name: NODE_ENV
                        valueFrom:
                            configMapKeyRef:
                                name: transvirex-config
                                key: NODE_ENV
                      - name: RABBITMQ_HOST
                        valueFrom:
                            configMapKeyRef:
                                name: transvirex-config
                                key: RABBITMQ_HOST
                      - name: RABBITMQ_PORT
                        valueFrom:
                            configMapKeyRef:
                                name: transvirex-config
                                key: RABBITMQ_PORT
                      - name: RABBITMQ_USER
                        valueFrom:
                            configMapKeyRef:
                                name: transvirex-config
                                key: RABBITMQ_USER
                      - name: RABBITMQ_PASSWORD
                        valueFrom:
                            configMapKeyRef:
                                name: transvirex-config
                                key: RABBITMQ_PASSWORD
                      - name: POSTGRES_HOST
                        valueFrom:
                            configMapKeyRef:
                                name: transvirex-config
                                key: POSTGRES_HOST
                      - name: POSTGRES_PORT
                        valueFrom:
                            configMapKeyRef:
                                name: transvirex-config
                                key: POSTGRES_PORT
                      - name: POSTGRES_USER
                        valueFrom:
                            configMapKeyRef:
                                name: transvirex-config
                                key: POSTGRES_USER
                      - name: POSTGRES_PASSWORD
                        valueFrom:
                            configMapKeyRef:
                                name: transvirex-config
                                key: POSTGRES_PASSWORD
                      - name: POSTGRES_DB
                        valueFrom:
                            configMapKeyRef:
                                name: transvirex-config
                                key: POSTGRES_DB
                      - name: LOG_LEVEL
                        valueFrom:
                            configMapKeyRef:
                                name: transvirex-config
                                key: LOG_LEVEL
                  ports:
                      - containerPort: 3000
                        name: http
                  resources:
                      requests:
                          cpu: 100m
                          memory: 64Mi
                      limits:
                          cpu: 500m
                          memory: 256Mi
---
apiVersion: v1
kind: Service
metadata:
    name: transvirex-stock
    namespace: production
    labels:
        app: transvirex-stock
spec:
    type: ClusterIP
    selector:
        app: transvirex-stock
    ports:
        - port: 3000
          targetPort: 3000
          name: http
          protocol: TCP
